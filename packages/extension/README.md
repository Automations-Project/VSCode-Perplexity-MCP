# Perplexity Internal MCP Extension

Native VS Code extension packaging for the Perplexity Internal MCP runtime. The extension embeds the MCP server, exposes a dashboard webview, and can generate `mcp.json` configs for Cursor, Windsurf, and Claude Desktop.
